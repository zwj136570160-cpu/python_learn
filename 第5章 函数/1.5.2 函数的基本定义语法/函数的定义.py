"""
- 目标
1.掌握函数的基础定义语法

- 函数的定义
def 函数名（传入参数）：
    函数体
    return 返回值
传入参数和return返回值，暂不需要考虑，可省略，后面章节学习

- 函数的调用
函数名（参数）
"""

# 定义一个函数，输出相关信息
def say_hi():
    print("hi,我喜欢楠楠！！！！")

# 调用函数，让定义的函数开始工作
say_hi()

"""
- 注意事项
1.参数如不需要，可以省略（后续章节讲解）
2.返回值如不需要，可以省略（后续章节讲解）
3.函数必须先定义后使用
"""

"""
- 总结
1.函数的定义语法
def 函数名（传入参数）：
    函数体
    return 返回值
2.函数使用步骤
· 先定义函数
· 后调用函数
3.注意事项：
· 参数不需要，可以省略
· 返回值不需要，可以省略
"""